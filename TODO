

The following needs some more attention:
 - When adding a equipment role is required, but is not mark as required in the API
 - Adding multiple values for roles with model equipment
 - Need to rewrite the way an Interface is saved
 - Rename model HardwareModel to EquipmentModel
